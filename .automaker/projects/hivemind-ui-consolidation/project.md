# Project: Hivemind UI Consolidation

## Goal
Move server URL override to Developer Settings only. Replace the bottom-panel util ticker dropdown with a hover popover showing live hivemind network stats (peers, capacity, connection). Remove the instances/peers panel from the board view. Remove All/Mine tabs. Consolidate everything into a single ticker popover.

