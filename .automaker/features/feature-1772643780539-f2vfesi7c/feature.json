{
  "category": "Bug Reports",
  "description": "Bug 1: Worktree Recovery Service Hardcodes Branch Names\n\nFile: worktree-recovery-service.ts lines 191, 243\n\nThe post-agent worktree recovery service hardcodes 'dev' as the base branch for PR creation and rebase operations:\n\n// Line 243 — hardcoded, doesn't read prBaseBranch from settings\nawait execFileAsync('gh', ['pr', 'create', '--base', 'dev', '--head', branchName, ...]);\n\n// Line 191 — hardcoded rebase target\nawait execFileAsync('git', ['rebase', 'origin/dev'], ...);\nThe main GitWorkflowService correctly reads prBaseBranch from settings, but the recovery service doesn't. This means:\n\nIf global prBaseBranch is 'dev' → recovery works (accidentally correct)\nIf global prBaseBranch is anything else → recovery creates PRs targeting wrong branch\nImpact on us: Since our project SHOULD use dev, this hardcoding actually helps us right now. But it's still a bug — it should read from settings.\n\nSeverity: Low for us (accidentally correct), Medium for other projects.Bug 1: Worktree Recovery Service Hardcodes Branch Names\nFile: worktree-recovery-service.ts lines 191, 243\n\nThe post-agent worktree recovery service hardcodes 'dev' as the base branch for PR creation and rebase operations:\n\n// Line 243 — hardcoded, doesn't read prBaseBranch from settings\nawait execFileAsync('gh', ['pr', 'create', '--base', 'dev', '--head', branchName, ...]);\n\n// Line 191 — hardcoded rebase target\nawait execFileAsync('git', ['rebase', 'origin/dev'], ...);\nThe main GitWorkflowService correctly reads prBaseBranch from settings, but the recovery service doesn't. This means:\n\nIf global prBaseBranch is 'dev' → recovery works (accidentally correct)\nIf global prBaseBranch is anything else → recovery creates PRs targeting wrong branch\nImpact on us: Since our project SHOULD use dev, this hardcoding actually helps us right now. But it's still a bug — it should read from settings.\n\nSeverity: Low for us (accidentally correct), Medium for other projects.",
  "title": "Bug 1: Worktree Recovery Service Hardcodes Branch Names\n\nFile: worktree-recovery-service.ts lines 19",
  "status": "done",
  "projectSlug": "bugs",
  "id": "feature-1772643780539-f2vfesi7c",
  "branchName": "feature/bug-1-worktree-recovery-service",
  "descriptionHistory": [
    {
      "description": "Bug 1: Worktree Recovery Service Hardcodes Branch Names\n\nFile: worktree-recovery-service.ts lines 191, 243\n\nThe post-agent worktree recovery service hardcodes 'dev' as the base branch for PR creation and rebase operations:\n\n// Line 243 — hardcoded, doesn't read prBaseBranch from settings\nawait execFileAsync('gh', ['pr', 'create', '--base', 'dev', '--head', branchName, ...]);\n\n// Line 191 — hardcoded rebase target\nawait execFileAsync('git', ['rebase', 'origin/dev'], ...);\nThe main GitWorkflowService correctly reads prBaseBranch from settings, but the recovery service doesn't. This means:\n\nIf global prBaseBranch is 'dev' → recovery works (accidentally correct)\nIf global prBaseBranch is anything else → recovery creates PRs targeting wrong branch\nImpact on us: Since our project SHOULD use dev, this hardcoding actually helps us right now. But it's still a bug — it should read from settings.\n\nSeverity: Low for us (accidentally correct), Medium for other projects.Bug 1: Worktree Recovery Service Hardcodes Branch Names\nFile: worktree-recovery-service.ts lines 191, 243\n\nThe post-agent worktree recovery service hardcodes 'dev' as the base branch for PR creation and rebase operations:\n\n// Line 243 — hardcoded, doesn't read prBaseBranch from settings\nawait execFileAsync('gh', ['pr', 'create', '--base', 'dev', '--head', branchName, ...]);\n\n// Line 191 — hardcoded rebase target\nawait execFileAsync('git', ['rebase', 'origin/dev'], ...);\nThe main GitWorkflowService correctly reads prBaseBranch from settings, but the recovery service doesn't. This means:\n\nIf global prBaseBranch is 'dev' → recovery works (accidentally correct)\nIf global prBaseBranch is anything else → recovery creates PRs targeting wrong branch\nImpact on us: Since our project SHOULD use dev, this hardcoding actually helps us right now. But it's still a bug — it should read from settings.\n\nSeverity: Low for us (accidentally correct), Medium for other projects.",
      "timestamp": "2026-03-04T17:03:00.540Z",
      "source": "initial"
    }
  ],
  "createdAt": "2026-03-04T17:03:00.540Z",
  "statusHistory": [
    {
      "from": null,
      "to": "backlog",
      "timestamp": "2026-03-04T17:03:00.540Z",
      "reason": "Feature created"
    },
    {
      "from": "backlog",
      "to": "done",
      "timestamp": "2026-03-04T20:28:18.261Z"
    }
  ],
  "featureType": "code",
  "completedAt": "2026-03-04T20:28:18.261Z"
}